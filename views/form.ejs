<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Photo Search</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <link rel="icon" type="image/png" href="favicon.png">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Roboto', Arial, sans-serif;
            background-color: #1f1f1f;
            color: #ffffff;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 50px;
            background-color: #2a2a2a;
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .header .logo {
            font-size: 1.5em;
            font-weight: bold;
            color: #ffffff;
        }

        .header .nav {
            display: flex;
            gap: 20px;
        }

        .header .nav a {
            text-decoration: none;
            color: #ffffff;
            font-size: 1em;
            padding: 10px 20px;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .header .nav a:hover {
            background-color: #444444;
        }

        .container {
            text-align: center;
            padding: 50px 20px;
        }

        .subtitle {
            font-size: 1.2em;
            color: #dcdcdc;
            margin-bottom: 40px;
        }

        .upload-form {
            background-color: #2a2a2a;
            padding: 30px;
            border-radius: 10px;
            display: inline-block;
            text-align: left;
            width: 100%;
            max-width: 500px;
            margin: 0 auto;
        }

        .upload-form label {
            font-size: 1em;
            margin-bottom: 10px;
            display: block;
            color: #ffffff;
        }

        .upload-form input[type="text"] {
            width: 100%;
            padding: 10px;
            margin: 10px 0 20px;
            font-size: 1em;
            color: #dcdcdc;
            background-color: #1f1f1f;
            border: 1px solid #444444;
            border-radius: 5px;
        }

        .upload-form input[type="text"]:focus {
            outline: none;
            border-color: #007BFF;
        }

        .upload-form button {
            padding: 15px 30px;
            font-size: 1em;
            background-color: #007BFF;
            color: #ffffff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            display: block;
            width: 100%;
        }

        .upload-form button:hover {
            background-color: #0056b3;
        }

        .footer {
            text-align: center;
            padding: 20px;
            background-color: #2a2a2a;
            color: #dcdcdc;
            font-size: 0.9em;
        }

        .footer a {
            color: #007BFF;
            text-decoration: none;
            margin: 0 10px;
        }

        .footer a:hover {
            text-decoration: underline;
        }

        @media (max-width: 768px) {
            .header {
                flex-direction: column;
                text-align: center;
            }

            .header .nav {
                flex-direction: column;
                gap: 10px;
            }

            .upload-form {
                width: 90%;
            }
        }
    </style>
</head>

<body>
    <div class="header">
        <div class="logo">PhotoUpscaler</div>
        <div class="nav">
            <a href="frontPage">Logout</a>
        </div>
    </div>

    <div class="container">
        <div class="subtitle">Search for images using a text description below.</div>

        <!-- Form with only text input for search -->
        <form class="upload-form" action="/search" method="POST">
            <label for="text-field">Enter Text:</label>
            <input type="text" id="text-field" name="text-field" placeholder="Enter your text" required>
            <button type="submit">Search</button>
        </form>
    </div>

    <div class="footer">
        <p>© 2024 PhotoUpscaler. All rights reserved.</p>
        <p><a href="/terms">Terms of Service</a> | <a href="/privacy">Privacy Policy</a></p>
    </div>
</body>

</html>