<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Photo Upscaler - Upload</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <link rel="icon" type="image/png" href="favicon.png">

    <style>
        body {
            font-family: 'Roboto', Arial, sans-serif;
            background-color: #1f1f1f;
            color: #ffffff;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 50px;
            background-color: #2a2a2a;
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .header .logo {
            font-size: 1.5em;
            font-weight: bold;
            color: #ffffff;
        }

        .header .nav a {
            text-decoration: none;
            color: #ffffff;
            font-size: 1em;
            padding: 10px 20px;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .header .nav a:hover {
            background-color: #444444;
        }

        .container {
            text-align: center;
            padding: 50px 20px;
        }

        .title {
            font-size: 2.5em;
            margin-bottom: 20px;
        }

        .subtitle {
            font-size: 1.2em;
            color: #dcdcdc;
            margin-bottom: 40px;
        }

        .upload-form {
            background-color: #2a2a2a;
            padding: 30px;
            border-radius: 10px;
            width: 100%;
            max-width: 500px;
            margin: 0 auto;
        }

        .upload-form label {
            font-size: 1em;
            margin-bottom: 10px;
            color: #ffffff;
        }

        .upload-form input[type="file"] {
            width: 100%;
            padding: 10px;
            font-size: 1em;
            background-color: #1f1f1f;
            border: 1px solid #444444;
            border-radius: 5px;
        }

        .upload-form input[type="file"]:focus {
            outline: none;
            border-color: #007BFF;
        }

        .upload-form button {
            padding: 15px 30px;
            font-size: 1em;
            background-color: #007BFF;
            color: #ffffff;
            border: none;
            border-radius: 5px;
            width: 100%;
        }

        .upload-form button:hover {
            background-color: #0056b3;
        }

        .footer {
            text-align: center;
            padding: 20px;
            background-color: #2a2a2a;
            color: #dcdcdc;
        }

        .footer a {
            color: #007BFF;
            text-decoration: none;
            margin: 0 10px;
        }

        .footer a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <!-- Header -->
    <div class="header">
        <div class="logo">PhotoUpscaler</div>
        <div class="nav">
            <a href="frontPage">Logout</a>
        </div>
    </div>

    <!-- Upload Section -->
    <div class="container">
        <div class="title">Upload Your Image</div>
        <div class="subtitle">Select an image to upscale using our cutting-edge AI technology.</div>

        <div class="mb-4">
            <img src="images/lowrestoupscale.png" class="img-fluid rounded" alt="Low Resolution Image">
        </div>

        <form class="upload-form" action="/upload" method="POST" enctype="multipart/form-data">
            <label for="image-upload">Choose an Image:</label>
            <input type="file" id="image-upload" name="image-upload" accept="image/*" required>
            <button type="submit" class="btn btn-primary mt-3">Upload and Upscale</button>
        </form>
    </div>

    <!-- Image Gallery -->
    <div class="container mt-5">
        <h2 class="text-center mb-4">Uploaded Images</h2>

        <div class="row">
            <% images.forEach(function(imageUrl, index) { %>
            <div class="col-12 d-flex justify-content-center mb-4">
                <div class="card w-50">
                    <img src="<%= imageUrl %>" class="card-img-top img-fluid" alt="Image">
                    <div class="card-body text-center">
                        <p class="card-text">Rank: <strong><%= index + 1 %></strong></p>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>
    </div>

    <!-- Footer -->
    <div class="footer">
        <p>© 2024 PhotoUpscaler. All rights reserved.</p>
        <p><a href="/terms">Terms of Service</a> | <a href="/privacy">Privacy Policy</a></p>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>

</html>