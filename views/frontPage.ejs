<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SmartScaler</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <link rel="icon" type="image/png" href="favicon.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Roboto', Arial, sans-serif;
            background-color: #1f1f1f;
            color: #ffffff;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 50px;
            background-color: #2a2a2a;
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .header .logo {
            font-size: 1.5em;
            font-weight: bold;
            color: #ffffff;
        }

        .header .nav {
            display: flex;
            gap: 20px;
        }

        .header .nav a,
        .header .nav button {
            text-decoration: none;
            color: #ffffff;
            background-color: #444444;
            font-size: 1em;
            padding: 10px 20px;
            border-radius: 5px;
            transition: background-color 0.3s;
            border: none;
            cursor: pointer;
        }

        .header .nav a:hover,
        .header .nav button:hover {
            background-color: #0000ff;
        }

        .hero {
            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)),
                url('lowrestoupscale.png') no-repeat center center/cover;
            height: 60vh;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            color: #ffffff;
        }

        .hero-content h1 {
            font-size: 3em;
            margin-bottom: 20px;
        }

        .hero-content p {
            font-size: 1.2em;
            margin-bottom: 30px;
            color: #dcdcdc;
        }

        .cta-button {
            padding: 15px 30px;
            background-color: #007BFF;
            color: #ffffff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .cta-button:hover {
            background-color: #0056b3;
        }

        .container {
            text-align: center;
            padding: 50px 20px;
        }

        .title {
            font-size: 2em;
            margin-bottom: 20px;
        }

        .subtitle {
            font-size: 1.2em;
            color: #dcdcdc;
            margin-bottom: 40px;
        }

        .footer {
            text-align: center;
            padding: 20px;
            background-color: #2a2a2a;
            color: #dcdcdc;
            font-size: 0.9em;
        }

        .footer a {
            color: #007BFF;
            text-decoration: none;
            margin: 0 10px;
        }

        .footer a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="header">
        <div class="logo">SmartScaler</div>
        <div class="nav">
            <% if (user) { %>
            <!-- Replaced plus button with Upload, Search, and Queue buttons -->
            <form action="/gallery" method="GET">
                <button type="submit">Upload</button>
            </form>
            <form action="/search" method="GET">
                <button type="submit">Search</button>
            </form>
            <form action="/queue" method="GET">
                <button type="submit">Queue</button>
            </form>
            <% } %>

            <% if (!user) { %>
            <form action="/login" method="GET">
                <button type="submit">Login</button>
            </form>
            <form action="/register" method="GET">
                <button type="submit">Register</button>
            </form>
            <% } else { %>
            <form action="#" method="GET">
                <button type="submit">Logout</button>
            </form>
            <% } %>
        </div>
    </div>

    <div class="hero">
        <div class="hero-content">
            <h1>SmartScaler</h1>
            <p>Revolutionize your images with AI-driven upscaling technology. Fast. Secure. Reliable.</p>
            <a href="register.html" class="cta-button">Get Started</a>
        </div>
    </div>

    <div class="footer">
        <p>© 2024 SmartScaler. All rights reserved.</p>
        <p><a href="/terms">Terms of Service</a> | <a href="/privacy">Privacy Policy</a></p>
    </div>
</body>

</html>